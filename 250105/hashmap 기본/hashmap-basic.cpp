#include <iostream>
#include <vector>
#include <unordered_map>

using namespace std;

int n;
string str;
vector<string> functions;
unordered_map<int, int> map;

int main() {
    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> str;
        if (isdigit(str)) {
            
        }
    }

    return 0;
}